# SKALA 4기 Claude 작업 지침

이 저장소에서는 `AGENTS.md`를 공용 작업 규칙의 기준으로 사용한다. 작업을 시작할 때 `AGENTS.md`와 `학습관리.md`를 먼저 읽고 따른다.

Codex도 같은 로컬 작업 폴더를 수정할 수 있으므로 다음을 지킨다.

- 작업 시작 전과 파일 저장 전에 `git status --short`와 대상 파일의 `git diff`를 확인한다.
- 자신이 만들지 않은 변경을 되돌리거나 덮어쓰지 않는다.
- 같은 파일을 동시에 수정하지 않는다. 작업 중 파일이 바뀌면 최신 내용을 다시 읽고 병합한다.
- 의미 있는 질문, 결정, 코드 변경, 검증 결과, 남은 문제를 `학습관리.md`에 간결하게 기록한다.
- 작업 완료 시 변경 파일과 실행한 검증을 사용자에게 알린다.
