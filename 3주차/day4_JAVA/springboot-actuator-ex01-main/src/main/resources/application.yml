spring:
  application:
    name: mvcactuator

server:
  port: 8080

management:
  endpoints:
    access:
      max-permitted: read-only
    web:
      exposure:
        include: health,info,metrics

  endpoint:
    health:
      show-details: always
